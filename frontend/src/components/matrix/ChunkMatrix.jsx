/**
 * ChunkMatrix.jsx
 * D3.js grid container for erasure code distribution visualization.
 * Rows = chunk labels (D1, D2, D3, D4, P1, P2).
 * Columns = SAT-01 through SAT-06.
 * Subscribes to chunk events, updates cells in real time.
 */
