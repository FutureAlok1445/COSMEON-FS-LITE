/**
 * MatrixCell.jsx
 * Individual D3-rendered cell for the chunk matrix.
 * Colors: blue=data, green=parity, red=missing, orange=corrupted, purple=reconstructing.
 * Tooltip on hover with chunk details (ID, SHA-256 prefix, size).
 */
