/**
 * SatelliteNode.jsx
 * Custom React Flow node for each satellite.
 * Shows: name, animated status pulse, chunk count, mini storage bar, orbit timer countdown.
 * Color-coded by status (green/yellow/red).
 */
